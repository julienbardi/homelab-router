# Secrets (never committed)
ddns/.ddns_confidential
caddy/env

# WireGuard private keys and runtime files
wireguard/*.key
wireguard/*.runtime

# Caddy ACME storage
caddy/data/

# Editor noise
*.swp
*.tmp
*~

# Dnsmasq cache change marker
.dnsmasq-cache-changed

# Folder for local tools (e.g. checkmake)
.tools/
/bin/caddy
